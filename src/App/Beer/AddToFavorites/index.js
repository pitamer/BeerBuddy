import React from 'react';
import { observer } from "mobx-react";
import './index.css';

function Modal() {
  return (
    <div className="Modal">
      Modal
    </div>
  );
}

export default observer(Modal);
